// banner 轮播
var bannerSwiper = new Swiper ('.banner .swiper-container', {
    loop: true,

    // 分页器
    pagination: '.swiper-pagination',
})

// 部门介绍左右滑动切换
var department_message = new Swiper ('.department-message .swiper-container', {
    // loop: true,
    autoHeight: true, //高度随内容变化
    
    // 分页器
    // pagination: '.swiper-pagination',
})










